# npm strict peer-dep resolution breaks on transitive optional peers
# (e.g. inngest's optional typescript peer). Use legacy resolution so
# `npm install` / Vercel builds don't ERESOLVE. Safe for this JS (non-TS) project.
legacy-peer-deps=true